Emitter.js is from https://github.com/component/emitter/blob/master/index.js


(Corresponding WebPack config should have the `emitter.js` path in it, e.g.:

```
  resolve: {
    root: [
      ...
      path.resolve('./external/emitter')
      ...
    ],
  }
```
